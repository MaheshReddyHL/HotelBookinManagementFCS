@*@model WebApplication1.Models.CustomerLogin
    @{
        ViewBag.Title = "CustLogin";
    }

    <h2>CustomerLogin</h2>

    <body class="backgrd">


        @using (Html.BeginForm("CustLogin", "Account", FormMethod.Post))
        {
            <div class="form-group">
                @Html.LabelFor(m => m.UserName)
                @Html.TextBoxFor(m => m.UserName, new { @class = "form-control" })
                @Html.ValidationMessageFor(m => m.UserName)
            </div>
            <div class="form-group">
                @Html.LabelFor(m => m.Password)
                @Html.TextBoxFor(m => m.Password, new { @class = "form-control", type = "password" })
                @Html.ValidationMessageFor(m => m.Password)
            </div>

            <div class="form-group">
                <input type="submit" value="Login" class="btn btn-primary" />
            </div>

            if (ViewBag.ErrorMessage != null)
            {
                <div class="alert alert-danger">@ViewBag.ErrorMessage</div>
            }
        }
    </body>*@






@*@model WebApplication1.Models.CustomerLogin
@{
    ViewBag.Title = "CustLogin";
}
<h2>Customer Login</h2>
<body class="backgrd">
    @using (Html.BeginForm("CustLogin", "Account", FormMethod.Post))
    {
        <div class="form-group">
            @Html.LabelFor(m => m.UserName)
            @Html.TextBoxFor(m => m.UserName, new { @class = "form-control" })
            @Html.ValidationMessageFor(m => m.UserName)
        </div>
        <div class="form-group">
            @Html.LabelFor(m => m.Password)
            @Html.TextBoxFor(m => m.Password, new { @class = "form-control", type = "password" })
            @Html.ValidationMessageFor(m => m.Password)
        </div>
        <div class="form-group">
            <input type="submit" value="Login" class="btn btn-primary" />
        </div>
        if (ViewBag.ErrorMessage != null)
        {
            <div class="alert alert-danger">@ViewBag.ErrorMessage</div>
        }
    }
</body>
<style>
    /* Full screen background image */
    .backgrd {
        background-image: url('../../models/images/bedroom-1330846 (1).jpg'); /* Add your 4K image URL here */
        background-size: cover; /* Ensures the image covers the entire background */
        background-position: center; /* Centers the image */
        background-repeat: no-repeat;
        height: 100vh; /* Full viewport height */
        display: flex;
        justify-content: center; /* Centers the content horizontally */
        align-items: center; /* Centers the content vertically */
        font-family: 'Arial', sans-serif;
    }

    /* Centering form */
    form {
        background-color: rgba(255, 255, 255, 0.8); /* Slightly transparent white background for form */
        padding: 30px;
        border-radius: 10px;
        box-shadow: 0px 0px 15px rgba(0, 0, 0, 0.2); /* Add shadow for a modern look */
        max-width: 400px;
        width: 100%;
    }

    h2 {
        text-align: center;
        color: #333;
        font-weight: bold;
    }

    .form-group {
        margin-bottom: 15px;
    }

    .form-control {
        width: 100%;
        padding: 10px;
        border-radius: 5px;
        border: 1px solid #ccc;
    }

    .btn-primary {
        background-color: #007bff;
        border: none;
        padding: 10px 15px;
        color: white;
        width: 100%;
        border-radius: 5px;
        cursor: pointer;
    }

        .btn-primary:hover {
            background-color: #0056b3;
        }

    .alert-danger {
        color: white;
        background-color: #dc3545;
        padding: 10px;
        margin-top: 15px;
        text-align: center;
        border-radius: 5px;
    }
</style>*@





@model WebApplication1.Models.AdminLogin
@{
    ViewBag.Title = "CustLogin";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewBag.Title</title>

    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet" />

    <style>
        /* Set background image */
        body {
            background-image: url('../../models/images/bedroom-1330846 (1).jpg'); /* Replace with actual URL */
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            height: 100vh;
        }

        /* Centering the form */
        main {
            max-width: 400px;
            margin: 50px auto;
            background-color: rgba(255, 255, 255, 0.9);
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
        }

        h2 {
            text-align: center;
            margin-bottom: 20px;
        }

        .btn-primary {
            background-color: #5a67d8; /* Violet color */
            border-color: #5a67d8;
        }

            .btn-primary:hover {
                background-color: #434190; /* Darker violet */
            }

        .alert-danger {
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <main>
        <h2>Customer Login</h2>
        @using (Html.BeginForm("CustLogin", "Account", FormMethod.Post))
        {
            <div class="form-group">
                @Html.LabelFor(m => m.UserName)
                @Html.TextBoxFor(m => m.UserName, new { @class = "form-control" })
                @Html.ValidationMessageFor(m => m.UserName)
            </div>
            <div class="form-group">
                @Html.LabelFor(m => m.Password)
                @Html.TextBoxFor(m => m.Password, new { @class = "form-control", type = "password" })
                @Html.ValidationMessageFor(m => m.Password)
            </div>
            <div class="form-group">
                <input type="submit" value="Login" class="btn btn-primary btn-block" />
            </div>

            if (ViewBag.ErrorMessage != null)
            {
                <div class="alert alert-danger">@ViewBag.ErrorMessage</div>
            }
        }
    </main>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>

